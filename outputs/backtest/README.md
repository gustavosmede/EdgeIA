# Backtest Outputs

Os artefatos do backtest migrado ficam nesta pasta:

- `trades.csv`
- `equity_curve.csv`
- `summary.json`
- `equity_curve.png` se `matplotlib` estiver disponível

Use `bash scripts/prepare_backtest.sh` para regenerar tudo.

